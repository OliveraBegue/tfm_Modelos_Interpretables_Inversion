# Bloque 2 — Primera iteración: EBM / LightGBM / Student

Notebook(s) de la metodología descrita en la memoria: entrenamiento y comparación de EBM, LightGBM y modelo Student (destilación), con validación Walk-Forward y optimización con Optuna.

**Output esperado:** modelos entrenados en `models/iteracion_1/`, métricas y gráficas SHAP en `reports/figures/`.
